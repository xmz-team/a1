instructions="remove log_reincarnation and experimental config, reconstruct gui, fix the problem that a1 will fail setuid when running under non-root users, remove the deb822 format so that sileo can parse the deb package normally."

#!/bin/bash

script_path="$(cd $(dirname "${BASH_SOURCE[0]}") && pwd)"

source ${script_path}/ci/env.sh

#"${script_path}/generate-version.sh" "a1ctl"

c++ -ObjC++ \
 $CXXFLAGS ${lib_path[@]} \
 -lzip \
 ${src_path}/src/cxxa1ctl.cc -o cxxa1ctl

#chown 0:0 cxxa1ctl
#chmod 4755 cxxa1ctl

